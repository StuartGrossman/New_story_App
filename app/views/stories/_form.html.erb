<%= form_for(@story, :html => { :multipart => true}) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :story_title %><br>
    <%= f.text_field :story_title %>
  </div>
  <div class="field">
    <%= f.label :story_address %><br>
    <%= f.text_field :story_address %>
  <div>
  </div class="feild">
    <%= f.label :story_text %><br>
    <%= f.text_area :story_text %>
  <div>
  <div class="field">
  <%= f.label :image %><br />
  <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
